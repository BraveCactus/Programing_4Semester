19. Порядок инициализации полей класса.
Правило: Поля инициализируются в порядке их объявления в классе, а не в порядке списка инициализации.

Пример:

cpp
class A {
    int x;
    int y;
public:
    A() : y(1), x(y) {} // x инициализируется первым, но y еще не инициализирован!
};