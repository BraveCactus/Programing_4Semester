76. Функции std::move и std::forward.
std::move:

Безусловно приводит к rvalue

Реализация: static_cast<T&&>

std::forward:

Условно приводит к rvalue (сохраняет категорию)

Используется с универсальными ссылками