26. Ключевое слово override.
Назначение: Явно указывает, что метод переопределяет виртуальный метод базового класса.

Преимущества:

Проверка компилятором

Улучшает читаемость

cpp
class Derived : public Base {
    void foo() override; // ошибка, если Base::foo() не виртуальная
};