53. Что такое частичная специализация шаблонного класса?
Определение: Специализация, где некоторые параметры остаются шаблонными.

Пример:

cpp
template <typename T, typename U>
class Pair { ... };

template <typename T>
class Pair<T, int> { ... }; // частичная специализация