56. Когда используем auto, а когда decltype для вывода типов?
auto:

Когда нужен простой вывод типа

В объявлениях переменных

В возвращаемых типах функций с trailing return type

decltype:

Когда нужен точный тип

В метапрограммировании

В шаблонах для зависимых типов