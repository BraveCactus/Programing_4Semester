55. Что делает decltype?
Назначение: Получает точный тип выражения без вычисления самого выражения.

Особенности:

Сохраняет ссылки и cv-квалификаторы

Для переменных дает объявленный тип

Для выражений дает тип результата